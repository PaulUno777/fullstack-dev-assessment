# Candidates client

Vite + React + TypeScript + Tailwind (pnpm).

```bash
pnpm install
pnpm dev
```

Build: `pnpm build`  
API URL: see `.env.example` (`VITE_API_URL`). Dev proxy forwards `/candidates` to `http://localhost:3000`.
